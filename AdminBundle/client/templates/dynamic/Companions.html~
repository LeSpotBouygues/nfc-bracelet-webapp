<template name="Companions">
 








  <div class="content">    
    <div class="row">
      <!-- left column -->
      <div class="col-md-2">
      </div>
      <div class="col-md-8">
        <!-- general form elements -->
        <div class="box box-primary">
          <div class="box-header with-border">
            <h3 class="box-title">Select a Companion for update</h3>
          </div>
          <!-- /.box-header -->
          <!-- form start -->
          <form role="form" id="findCompanionForUpdate">
            <div class="box-body">
	      <div class="form-group">
                <label for="exampleInputFile">Select a member</label>
		<select class="form-control" id="selectCompanionForUpdate"
			name="selectCompanionForUpdate">
	      {{#each companionsFull}}
	      <option value="{{_id}}">{{firstName}} {{lastName}}</option>
	      {{/each}}
	    </select>
		
              </div>
	    </div>
            <!-- /.box-body -->
	    
            <div class="box-footer">
              <button type="submit" class="btn btn-primary">Select Workers For Update</button>
            </div>    
          </form>
        </div>
      </div>
    </div>

    


    <div class="row">
      <div class="col-lg-2">
      </div>
      <div class="col-lg-8">
	<div class="box">
	  <div class="box-header with-border">
	    <h3 class="box-title">Workers List</h3><br/><br/>
	  </div>
	  <div class="box-body no-padding">
	    <table class="table table-bordered">
	      <tbody>
		<tr>
		  <th>FirstName</th>
		  <th>LastName</th>
		  <th>Surname</th>
		  <th>Nationality</th>
		  <th>Company</th>
		  <th>Position</th>
		  <th>Work Permit</th>
		  <th>Expiration</th>
		  <th>Vacation Start</th>
		  <th>Vacation End</th>
		  <th>Action</th>
		</tr>
		
		{{#each companions}}
		   {{> Companion}}
		{{/each}}
	      </tbody>
	    </table>
	  </div>
	  
	  <div class="box-footer clearfix">
	    <div class="box-tools">
	      <ul class="pagination pagination-sm no-margin pull-right">
		<li id="prevCompanions"><a href="">Previous</a></li>		
		<li><a id="nextCompanions" href="">Next</a></li>
				<li>
				  <select id="pageSelected" class="form-control">
				    {{#each pagination}}

				    {{#if this != 0}}
				    <option value="{{this}}">{{this}}</option>
				    {{/if}}
				    {{/each}}
				    
				  </select> 
				  
				  
				</li>
				
	      </ul>
	    </div>
	  </div>
	</div>
      </div>
    </div>
  </div>
</template>

<template name="Companion">
  <tr>
    <td>{{firstName}}</td>
    <td>{{lastName}}</td>
    <td>{{aliasName}}</td>
    <td>{{nationality}}</td>
    <td>{{company}}</td>
    <td>{{position}}</td>
    <td>{{workPermit}}</td>
    <td>{{formatDate expirationDate}}</td>
    <td>{{formatDate vacationStart}}</td>
    <td>{{formatDate vacationEnd}}</td>
    <td><a href="/admin/companion/{{_id}}/update" class="btn bg-olive" type="button">Modify</a></td>
  </tr>
</template>


<template name="SearchCompanions">
  <tr>
    <td>{{firstName}}</td>
    <td>{{lastName}}</td>
    <td>{{company}}</td>
    <td>{{position}}</td>
    <td><a href="/admin/companion/{{_id}}/update" class="btn bg-olive" type="button">Modify</a></td>
  </tr>
</template>
